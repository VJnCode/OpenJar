server:
  port: 8087

spring:
  application:
    name: social-service
  datasource:
    url: jdbc:mysql://localhost:3306/OpenJar_Social?createDatabaseIfNotExist=true
    username: root
    password: 1234
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

# Swagger/OpenAPI Configuration
springdoc:
  api-docs:
    path: /v3/api-docs